[project]
name = "rstrip"
version = "0.3.0"
description = "command line tool: rstrip"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
rstrip = "rstrip.rstrip:main"
